/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "127.0.0.1",
      "source.unsplash.com",
      "m.media-amazon.com" 
    ],
  },
};

export default nextConfig;
